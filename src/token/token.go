package token

// Generate : Generates a new token with Claims.
func Generate(claims map[string]interface{}) (string, error) {
	return "", nil
}

// Decode : Decodes a given token.
func Decode(token string) (map[string]interface{}, error) {
	return nil, nil
}
